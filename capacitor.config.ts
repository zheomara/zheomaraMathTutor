import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zheomara.mathtutor',
  appName: 'Zheomara Math Tutor',
  webDir: 'out'
};

export default config;
